# ------------------------------------------------
# 1. 보안 (가장 중요!!! 절대 지우지 마세요)
# ------------------------------------------------
# Streamlit 비밀키 파일 (API Key, Google Auth 정보 포함됨)
.streamlit/secrets.toml

# 혹시 루트 경로에 실수로 둔 비밀 파일들
secrets.toml
.env

# 구글 클라우드에서 다운받았던 JSON 키 파일 (실수로 올리지 않게 차단)
*.json

# ------------------------------------------------
# 2. Python & 가상환경 (필요 없는 파일들)
# ------------------------------------------------
# 컴파일된 캐시 파일들 (용량만 차지함)
__pycache__/
*.py[cod]
*$py.class

# 가상환경 폴더 (내 컴퓨터 설정이므로 올리면 안 됨)
# 본인이 사용하는 가상환경 폴더명이 있다면 여기에 추가하세요.
venv/
.venv/
env/
.env/

# ------------------------------------------------
# 3. OS & 에디터 설정 파일 (지저분한 파일 제거)
# ------------------------------------------------
# Mac OS 썸네일 파일
.DS_Store

# Windows 썸네일 파일
Thumbs.db

# VS Code 설정 폴더 (개인 설정이라 공유 불필요)
.vscode/